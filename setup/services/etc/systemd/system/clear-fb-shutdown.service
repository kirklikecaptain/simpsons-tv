[Unit]
Description=Clear framebuffer on shutdown
DefaultDependencies=no
Before=shutdown.target reboot.target halt.target

[Service]
Type=oneshot
ExecStart=/bin/sh -c 'cat /dev/zero > /dev/fb0 2>/dev/null || true'

[Install]
WantedBy=halt.target reboot.target shutdown.target
