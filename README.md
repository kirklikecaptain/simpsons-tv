# Simpsons 3.5" TV

## Setup

Install Raspbian Trixie 64 bit (no desktop)

```bash
sudo apt update && sudo apt upgrade

sudo apt install git

git clone https://github.com/kirklikecaptain/simpsons-tv.git

cd simpsons-tv
```
